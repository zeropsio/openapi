zerops:
  - setup: openapi
    build:
      base: nodejs@22
      prepareCommand:
        - sudo npm i -g @redocly/cli
      buildCommands:
        - redocly docs openapi.yml --output index.html
      deployFiles:
        - index.html
    run:
      base: static
