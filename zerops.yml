zerops:
  - setup: openapi
    build:
      base: nodejs@22
      prepareCommands:
        - sudo npm i -g @redocly/cli
      buildCommands:
        - redocly build-docs openapi.yml --output index.html
      deployFiles:
        - index.html
    run:
      base: static
